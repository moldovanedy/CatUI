//we use C++ naming conventions here, so ignore naming warnings
// ReSharper disable InconsistentNaming

namespace CatUI.Platform.Linux
{
    //temporarily empty; will have calls to the native libcatui library
    internal static class NativeInterop
    {
    }
}
